load("@aspect_rules_js//js:defs.bzl", "js_library")
load("@npm//:defs.bzl", "npm_link_all_packages")

exports_files(["package.json"])

npm_link_all_packages(name = "node_modules")

alias(
    name = "pkg",
    actual = ":prettier_srcs",
    visibility = ["//visibility:public"],
)

js_library(
    name = "prettier_srcs",
    srcs = glob([
        "**/*.js",
        "**/*.json",
    ], allow_empty = True),
    visibility = ["//visibility:public"],
)
